export const metadata = {
  title: "Tasks",
  description:
    "Animated Kanban task radar for tracking backlog, in-progress work, review items, sprint points, and priorities.",
};

export default function TasksLayout({ children }) {
  return children;
}