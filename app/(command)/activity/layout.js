export const metadata = {
  title: "Activity",
  description:
    "GitHub-style engineering activity timeline for commits, pull requests, issues, reviews, and deployments.",
};

export default function ActivityLayout({ children }) {
  return children;
}