export const metadata = {
  title: "Insights",
  description:
    "Productivity analytics cockpit showing commit velocity, focus hours, bug trends, task closure, and sprint intelligence.",
};

export default function InsightsLayout({ children }) {
  return children;
}