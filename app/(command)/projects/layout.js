export const metadata = {
  title: "Projects",
  description:
    "Project fleet management interface showing health, progress, priority, stack, deadlines, bugs, and commits.",
};

export default function ProjectsLayout({ children }) {
  return children;
}